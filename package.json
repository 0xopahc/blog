{
  "name": "5tv-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "prestart": "node scripts/sync-public.mjs",
    "predev": "node scripts/sync-public.mjs",
    "predeploy": "node scripts/sync-public.mjs",
    "precheck": "node scripts/sync-public.mjs",
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "wrangler deploy --dry-run",
    "sync": "node scripts/sync-public.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.116.0"
  }
}
