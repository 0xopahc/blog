{
  "name": "5tv-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "prestart": "node scripts/sync-public.mjs",
    "predev": "node scripts/sync-public.mjs",
    "predeploy": "node scripts/sync-public.mjs",
    "precheck": "node scripts/sync-public.mjs",
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy -e production",
    "tail": "wrangler tail -e production",
    "check": "wrangler deploy -e production --dry-run",
    "sync": "node scripts/sync-public.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.116.0"
  }
}
